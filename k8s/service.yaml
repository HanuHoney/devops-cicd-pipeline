apiVersion: v1
kind: Service
metadata:
  name: devops-cicd-pipeline-svc
  labels:
    app: devops-cicd-pipeline
spec:
  type: NodePort
  selector:
    app: devops-cicd-pipeline
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080